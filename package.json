{
  "name": "northbear-hsk-zombie",
  "version": "0.2.0",
  "private": true,
  "description": "HSK vocabulary arcade game - NorthBear",
  "scripts": {
    "test": "vitest run",
    "build": "esbuild src/main.js --bundle --format=iife --outfile=dist/app.js",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^2.1.0"
  }
}
